__pycache__/
*.pyc
.venv/
venv/
.env
.env.local
.env.*.local
instance/
node_modules/

# Persistent clones of registered repos (REPO_CLONE_ROOT, see config.py) —
# defaults to here when no external storage is mounted over it.
/data/
app/static/dist/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Logs
*.log

# Docker Compose's standard local-override mechanism — machine-specific,
# never meant to be committed (e.g. pointing `web` at a different
# DATABASE_URL than .env's, for a local Postgres instance outside compose).
docker-compose.override.yml

